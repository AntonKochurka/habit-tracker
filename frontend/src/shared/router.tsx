import { createBrowserRouter } from "react-router-dom";

export let router = createBrowserRouter([
    {
        path: "/",
        element: <></>, // todo: Layout with Outlet
        children: [
            
            
        ]
    }
])